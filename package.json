{
  "name": "lift-tracker",
  "version": "1.0.0",
  "description": "Personal workout tracking app with Apple Health integration",
  "private": true,
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios",
    "ios": "cap sync ios && cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0",
    "@capacitor-community/health": "^3.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
